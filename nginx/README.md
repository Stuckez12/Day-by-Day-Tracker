Added max body size (10MB) to allow larger files to be transfered and stored

---------------------------------------------------------------------------------------------------
Base configuration from base-nginx.conf
